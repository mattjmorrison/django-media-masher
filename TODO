
1. Template tag that accepts list of files and returns the hashed name
    a. Template tag must create file if it doesn't exist, or always when development
    b. OR - have the template tag include groups by name and have
            a mash.py or something to contain the actual files to be mashed...
2. utility to take list of forms and/or formsets and mash media from all
3. support for css
4. don't let css and js be mixed


